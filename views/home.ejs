<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home Page</title>
    <style>
        body {
            font-family: 'Roboto Mono', 'JetBrains Mono', 'IBM Plex Mono', monospace;
        }

        button:hover {
            cursor: pointer;
        }
    </style>
</head>

<body>
    <h1>URL SHORTNER</h1>
    <% if(locals.id) {%>
        <p>URL Generated : http://localhost:8001/<%= id %>
        </p>
        <% } %>
            <form action="/" method="POST">
                <div>
                    <label for="">Enter Your Original URL</label>
                    <input type="text" name="url" id="" placeholder="https://example.com">
                    <button type="submit">Generate</button>
                </div>
            </form>

            <% if(locals.urls) {%>
                <table>
                    <thead>
                        <th>S.No</th>
                        <th>Short ID</th>
                        <th>Redirect</th>
                        <th>Clicks</th>
                    </thead>
                    <tbody>
                        <% urls.forEach((url, index)=> { %>
                            <tr>
                                <td>
                                    <%= index + 1 %>
                                </td>
                                <td>
                                    <%= url.shortId %>
                                </td>
                                <td><a href="/<%= url.shortId %>" target="_blank">Visit</a></td>
                                <td>
                                    <%= url.visitHistory.length %>
                                </td>
                            </tr>
                            <% }) %>
                    </tbody>

                </table>

                <% } %>
</body>

</html>